using System;
using System.Collections.Generic;
using Utilities;

namespace Task4
{
    public class SubTask5 : BaseTask
    {
        public override void Start()
        {
            base.Start();
            var Fibonacci = new List<int>(15);
            for (var i = 0; i < Fibonacci.Count; i++)
            {
                

            }

            foreach (var b in Fibonacci)
            {
                Console.WriteLine(b);
            }
        }
    }
}